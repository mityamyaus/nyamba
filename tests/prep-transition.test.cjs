const fs=require('node:fs'),vm=require('node:vm'),test=require('node:test'),assert=require('node:assert/strict');
function env(){const c=vm.createContext({structuredClone,console,localStorage:{setItem(){}}});for(const f of ['data.js','catalog.js','planner.js','prep-ui.js','live-catalog.js','builder.js'])vm.runInContext(fs.readFileSync('dist/'+f,'utf8'),c);vm.runInContext(fs.readFileSync('dist/app.js','utf8').split("$('#close').onclick")[0],c);return c}
test('manual menus can switch between all prep rhythms without conflicting batch selections',()=>{vm.runInContext(`for(const from of [1,3,4])for(const to of [1,3,4]){if(from===to)continue;const settings=normalizeSettings({...state,prepDays:from,allergies:[['egg'],[]],cuisines:['european']});const original=buildWeek(settings);const manual=manualSelection(original,0,1,original.meals[0][1].id);const before=JSON.stringify(manual);const next=changePrepDays(manual,to);if(JSON.stringify(manual)!==before)throw Error('mutated settings');if(Object.keys(next.manualMeals).length)throw Error('stale pins');const result=buildWeek(next);if(result.sessions.length!==Math.ceil(7/to))throw Error('session count');for(const row of result.meals)for(const meal of row){if(!dietaryAllowed(recipes[meal.id],next))throw Error('lost allergy');if(Math.abs(meal.kcal-result.totalKcal*shares[row.indexOf(meal)])>0.01)throw Error('lost calories')}}`,env())});
test('selecting the existing rhythm preserves manual choices and budget mode remains budget',()=>{vm.runInContext(`const s=normalizeSettings({...state,prepDays:3,menuMode:'manual',manualMeals:{'0-1':1}});if(JSON.stringify(changePrepDays(s,3))!==JSON.stringify(s))throw Error('same rhythm erased choices');if(changePrepDays({...s,menuMode:'budget'},4).menuMode!=='budget')throw Error('lost budget mode')`,env())});
